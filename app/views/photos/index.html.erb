<div class="container">
  <h2>All Photos</h2>
  <ul>
    <% @photos.each do |photo| %>
    <li><%= link_to photo.name, photo_path(photo)%><br>
    <% end %>
  </ul>
  <h4><%= link_to "Add a New Photo", new_photo_path %></h4>
</div>
